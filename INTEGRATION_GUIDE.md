# دليل ربط Frontend بالـ Backend

## ✅ ما تم إنجازه

تم ربط جميع الصفحات الرئيسية والـ Dashboard بالـ Backend API.

### الصفحات الرئيسية (Frontend → Backend)

1. **ServiceRequest (طلب الخدمة)**
   - ✅ يرسل البيانات للـ Backend عند الإرسال
   - ✅ يتم حفظ الطلب في MongoDB
   - ✅ يظهر تلقائياً في Dashboard → Orders

2. **Recruitment (التوظيف)**
   - ✅ يرسل البيانات للـ Backend عند الإرسال
   - ✅ يتم حفظ طلب التوظيف في MongoDB
   - ✅ يظهر تلقائياً في Dashboard → Applications

3. **Contact (التواصل)**
   - ✅ يرسل البيانات للـ Backend عند الإرسال
   - ✅ يتم حفظ الرسالة في MongoDB
   - ✅ يظهر تلقائياً في Dashboard → Messages

4. **Login (تسجيل الدخول)**
   - ✅ يتصل بالـ Backend API
   - ✅ يحفظ JWT token في localStorage
   - ✅ يتحقق من المستخدم تلقائياً عند تحميل التطبيق

### صفحات Dashboard (Backend → Frontend)

1. **Overview**
   - ✅ يحمّل الإحصائيات من API
   - ✅ يعرض الطلبات الأخيرة من Backend
   - ✅ الرسوم البيانية تستخدم بيانات حقيقية

2. **Orders**
   - ✅ يحمّل جميع الطلبات من Backend
   - ✅ يعرض الطلبات الجديدة تلقائياً
   - ✅ التعديل والحذف يعمل مع Backend

3. **Customers**
   - ✅ يحمّل العملاء من Backend
   - ✅ التعديل والحذف يعمل مع Backend

4. **Workers**
   - ✅ يحمّل العاملات من Backend
   - ✅ التعديل والحذف يعمل مع Backend

5. **Applications**
   - ✅ يحمّل طلبات التوظيف من Backend
   - ✅ قبول/رفض الطلبات يعمل مع Backend
   - ✅ قبول الطلبية يضيف عاملة تلقائياً

6. **Messages**
   - ✅ يحمّل الرسائل من Backend
   - ✅ الرد والأرشفة والحذف يعمل مع Backend

## 🔧 الإعداد المطلوب

### 1. إعداد Backend

```bash
cd backend
npm install
# أنشئ ملف .env وأضف معلومات MongoDB
npm run dev
```

### 2. إعداد Frontend

أنشئ ملف `.env` في جذر المشروع:
```env
VITE_API_URL=http://localhost:5000/api
```

### 3. إنشاء مستخدم Admin

بعد تشغيل Backend، أنشئ مستخدم admin من MongoDB Compass أو Postman:

```json
{
  "name": "مدير النظام",
  "email": "admin@cleaning.com",
  "password": "admin123",
  "role": "admin"
}
```

**ملاحظة:** كلمة المرور ستُشفّر تلقائياً.

## 📊 كيف يعمل النظام

### تدفق البيانات:

1. **طلب خدمة من الصفحة الرئيسية:**
   ```
   ServiceRequest → createOrder API → MongoDB → يظهر في Dashboard/Orders
   ```

2. **طلب توظيف:**
   ```
   Recruitment → createApplication API → MongoDB → يظهر في Dashboard/Applications
   ```

3. **رسالة تواصل:**
   ```
   Contact → createMessage API → MongoDB → يظهر في Dashboard/Messages
   ```

4. **Dashboard يعرض البيانات:**
   ```
   Dashboard Pages → fetchOrders/fetchApplications/fetchMessages API → MongoDB → Frontend
   ```

## 🔄 Auto-Refresh

البيانات الجديدة تظهر تلقائياً في Dashboard عند:
- تحميل الصفحة
- إعادة تحميل البيانات (refresh)

**لإضافة Auto-Refresh كل X ثواني:**
```javascript
useEffect(() => {
  const interval = setInterval(() => {
    dispatch(fetchOrders());
  }, 30000); // كل 30 ثانية
  
  return () => clearInterval(interval);
}, [dispatch]);
```

## 🐛 استكشاف الأخطاء

### البيانات لا تظهر في Dashboard:
1. تأكد من أن Backend يعمل على port 5000
2. تأكد من أن MongoDB متصل
3. افتح Developer Console وابحث عن أخطاء
4. تأكد من أن token موجود في localStorage

### خطأ CORS:
- تأكد من أن `FRONTEND_URL` في `.env` صحيح
- تأكد من أن Backend يعمل

### خطأ 401 Unauthorized:
- تأكد من تسجيل الدخول
- تأكد من أن token صحيح
- جرب تسجيل الخروج والدخول مرة أخرى

## 📝 ملاحظات

- جميع البيانات تُحفظ في MongoDB Atlas
- البيانات الجديدة تظهر تلقائياً عند تحميل Dashboard
- يمكن إضافة WebSocket أو Polling لتحديث تلقائي فوري
- جميع العمليات (Create, Read, Update, Delete) تعمل مع Backend

