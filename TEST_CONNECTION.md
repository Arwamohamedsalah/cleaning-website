# دليل اختبار الاتصال بين Frontend و Backend

## ✅ خطوات الاختبار

### 1. اختبار Backend

#### أ. تأكد من أن Backend يعمل:
```bash
cd backend
npm install
npm run dev
```

يجب أن ترى:
```
Server running in development mode on port 5000
MongoDB Connected: ...
```

#### ب. اختبر Health Check:
افتح المتصفح واذهب إلى:
```
http://localhost:5000/api/health
```

يجب أن ترى:
```json
{
  "success": true,
  "message": "Server is running"
}
```

### 2. اختبار Frontend

#### أ. تأكد من ملف .env:
أنشئ ملف `.env` في جذر المشروع:
```env
VITE_API_URL=http://localhost:5000/api
```

#### ب. شغّل Frontend:
```bash
npm run dev
```

### 3. اختبار تسجيل الدخول

1. اذهب إلى `/login`
2. استخدم بيانات Admin (التي أنشأتها في MongoDB)
3. يجب أن يتم تسجيل الدخول بنجاح

### 4. اختبار إرسال بيانات من الصفحة الرئيسية

#### أ. اختبار طلب خدمة:
1. اذهب إلى `/service-request`
2. املأ النموذج وأرسله
3. اذهب إلى Dashboard → Orders
4. يجب أن يظهر الطلب الجديد

#### ب. اختبار طلب توظيف:
1. اذهب إلى `/recruitment`
2. املأ النموذج وأرسله
3. اذهب إلى Dashboard → Applications
4. يجب أن يظهر الطلب الجديد

#### ج. اختبار رسالة تواصل:
1. اذهب إلى `/contact`
2. املأ النموذج وأرسله
3. اذهب إلى Dashboard → Messages
4. يجب أن تظهر الرسالة الجديدة

### 5. اختبار Dashboard

#### أ. Overview:
- يجب أن تظهر الإحصائيات من قاعدة البيانات
- يجب أن تظهر الطلبات الأخيرة

#### ب. Orders:
- يجب أن تظهر جميع الطلبات
- جرب التعديل والحذف

#### ج. Customers:
- يجب أن تظهر جميع العملاء
- جرب التعديل والحذف

#### د. Workers:
- يجب أن تظهر جميع العاملات
- جرب التعديل والحذف

#### ه. Applications:
- يجب أن تظهر جميع طلبات التوظيف
- جرب القبول/الرفض/الحذف

#### و. Messages:
- يجب أن تظهر جميع الرسائل
- جرب الرد والأرشفة والحذف

## 🐛 استكشاف الأخطاء

### خطأ: "Cannot connect to MongoDB"
**الحل:**
1. تأكد من أن ملف `.env` في مجلد `backend` موجود ويحتوي على `MONGODB_URI`
2. تأكد من أن Connection String صحيح
3. تأكد من أن IP Address مضاف في MongoDB Atlas Network Access

### خطأ: "CORS error"
**الحل:**
1. تأكد من أن `FRONTEND_URL` في `.env` صحيح
2. تأكد من أن Backend يعمل على port 5000
3. تأكد من أن Frontend يعمل على port 3000

### خطأ: "401 Unauthorized"
**الحل:**
1. تأكد من تسجيل الدخول
2. تأكد من أن token موجود في localStorage
3. جرب تسجيل الخروج والدخول مرة أخرى

### البيانات لا تظهر في Dashboard
**الحل:**
1. افتح Developer Console (F12)
2. ابحث عن أخطاء في Console
3. تأكد من أن Backend يعمل
4. تأكد من أن MongoDB متصل
5. تحقق من Network tab في Developer Tools

## 📝 ملاحظات مهمة

1. **يجب أن يكون Backend يعمل قبل تشغيل Frontend**
2. **يجب أن يكون MongoDB Atlas متصل**
3. **يجب أن يكون لديك مستخدم Admin في قاعدة البيانات**
4. **جميع البيانات تُحفظ في MongoDB Atlas**

## ✅ Checklist

- [ ] Backend يعمل على port 5000
- [ ] MongoDB متصل
- [ ] Frontend يعمل على port 3000
- [ ] ملف `.env` موجود في جذر المشروع
- [ ] ملف `.env` موجود في مجلد `backend`
- [ ] يمكن تسجيل الدخول
- [ ] البيانات تظهر في Dashboard
- [ ] يمكن إرسال طلب خدمة
- [ ] يمكن إرسال طلب توظيف
- [ ] يمكن إرسال رسالة تواصل

