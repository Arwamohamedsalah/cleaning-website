# 🚀 تشغيل الـ Backend Server

## المشكلة:
الـ backend server غير شغال، لذلك لا يمكن جلب بيانات المساعدات من قاعدة البيانات.

## الحل السريع:

### 1. افتح Terminal جديد

### 2. اذهب إلى مجلد backend:
```bash
cd backend
```

### 3. شغّل الـ server:
```bash
npm start
```

أو للتطوير (مع إعادة التشغيل التلقائي):
```bash
npm run dev
```

### 4. يجب أن ترى رسالة:
```
Server running in development mode on port 3001
```

### 5. الآن أعد تحميل صفحة المساعدات في المتصفح

---

## ملاحظات مهمة:

- **يجب أن يكون الـ backend server شغال دائماً** أثناء استخدام الموقع
- الـ server يعمل على: `http://localhost:3001`
- إذا ظهرت رسالة خطأ، تأكد من:
  - تثبيت المكتبات: `npm install` في مجلد backend
  - وجود ملف `.env` في مجلد backend
  - أن MongoDB Atlas متصل

---

## إذا لم يعمل:

1. تحقق من أن MongoDB متصل:
   ```bash
   cd backend
   npm run test:db
   ```

2. تحقق من ملف `.env`:
   - يجب أن يحتوي على `MONGODB_URI`
   - يجب أن يحتوي على `PORT=3001` (أو سيستخدم 3001 افتراضياً)

3. تأكد من تثبيت المكتبات:
   ```bash
   cd backend
   npm install
   ```

